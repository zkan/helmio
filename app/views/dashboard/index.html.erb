<div class="p-6">
  <h1 class="text-2xl font-bold mb-6">Dashboard</h1>

  <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
    <div class="bg-white p-6 rounded-lg shadow">
      <p class="text-sm text-gray-500">Total Gross Profit</p>
      <p class="text-3xl font-bold text-green-600"><%= number_to_currency(@total_gross_profit, unit: "THB", format: "%u %n", precision: 0) %></p>
    </div>

    <div class="bg-white p-6 rounded-lg shadow">
      <p class="text-sm text-gray-500">Total Revenue</p>
      <p class="text-3xl font-bold text-blue-600"><%= number_to_currency(@total_revenue, unit: "THB", format: "%u %n", precision: 0) %></p>
    </div>

    <div class="bg-white p-6 rounded-lg shadow">
      <p class="text-sm text-gray-500">Total Margin</p>
      <p class="text-3xl font-bold text-purple-600"><%= number_to_percentage(@total_margin, precision: 1) %></p>
    </div>
  </div>

  <h2 class="text-xl font-bold mb-4">By Site</h2>
  <div class="grid grid-cols-1 gap-4">
    <% @sites.each do |site_data| %>
      <div class="bg-white p-6 rounded-lg shadow">
        <h3 class="text-lg font-semibold mb-4"><%= site_data[:site].name %></h3>
        <div class="grid grid-cols-3 gap-4">
          <div>
            <p class="text-sm text-gray-500">Gross Profit</p>
            <p class="text-xl font-bold text-green-600"><%= number_to_currency(site_data[:gross_profit], unit: "THB", format: "%u %n", precision: 0) %></p>
          </div>
          <div>
            <p class="text-sm text-gray-500">Revenue</p>
            <p class="text-xl font-bold text-blue-600"><%= number_to_currency(site_data[:revenue], unit: "THB", format: "%u %n", precision: 0) %></p>
          </div>
          <div>
            <p class="text-sm text-gray-500">Margin</p>
            <p class="text-xl font-bold text-purple-600"><%= number_to_percentage(site_data[:margin], precision: 1) %></p>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
